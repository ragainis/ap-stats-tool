{
  "name": "ap-stats-tool",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:preview": "npm run build && npx -y netlify-cli deploy --dir=dist --no-build",
    "deploy": "npm run build && npx -y netlify-cli deploy --prod --dir=dist --no-build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.0"
  }
}
