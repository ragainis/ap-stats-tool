export const procedures = [
  { id:"1pz", name:"1-Proportion z", nodeId:"inf-prop", color:"#dc2626",
    when:"Testing or estimating a single population proportion p.",
    h0:"p = p₀", ha:"p ≠ / < / > p₀",
    conditions:["Random sample or random assignment","Large Counts: np₀ ≥ 10 and n(1−p₀) ≥ 10 [test] / np̂ ≥ 10 and n(1−p̂) ≥ 10 [CI]","Independent: n < 10% of population"],
    testStat:"z = (p̂ − p₀) / √(p₀(1−p₀)/n)",
    ci:"p̂ ± z* · √(p̂(1−p̂)/n)",
    calc:"1-PropZTest(p₀, x, n, direction) / 1-PropZInt(x, n, C)",
    conclude:"Since p = ___ is [< / ≥] α, we [reject / FTR] H₀. [Convincing / not convincing] evidence that [Hₐ context].",
    mistake:"Using p̂ in the test's SE (should be p₀) or p₀ in the CI's SE (should be p̂)." },
  { id:"2pz", name:"2-Proportion z", nodeId:"inf-prop", color:"#dc2626",
    when:"Comparing two population proportions p₁ − p₂.",
    h0:"p₁ = p₂ (or p₁ − p₂ = 0)", ha:"p₁ ≠ / < / > p₂",
    conditions:["Random: both samples random","Large Counts: n₁p̂c ≥ 10, n₁(1−p̂c) ≥ 10, same for group 2 [test] / use separate p̂'s [CI]","Independent: both n < 10% of their populations, groups independent"],
    testStat:"z = (p̂₁ − p̂₂) / √(p̂c(1−p̂c)(1/n₁ + 1/n₂))   where p̂c = (x₁+x₂)/(n₁+n₂)",
    ci:"(p̂₁ − p̂₂) ± z* · √(p̂₁(1−p̂₁)/n₁ + p̂₂(1−p̂₂)/n₂)",
    calc:"2-PropZTest / 2-PropZInt",
    conclude:"Since p = ___ is [< / ≥] α, we [reject / FTR] H₀. [Convincing / not convincing] evidence of a difference in [context].",
    mistake:"Using pooled p̂c in the CI (only pooling in the test), or failing to check Large Counts for both groups." },
  { id:"1t", name:"1-Sample t", nodeId:"inf-means", color:"#dc2626",
    when:"Testing or estimating a single population mean μ.",
    h0:"μ = μ₀", ha:"μ ≠ / < / > μ₀",
    conditions:["Random sample or assignment","Normal: n ≥ 30 (CLT) or population ~normal with no strong skew/outliers","Independent: n < 10% of population"],
    testStat:"t = (x̄ − μ₀) / (s / √n)     df = n − 1",
    ci:"x̄ ± t* · (s / √n)     df = n − 1",
    calc:"T-Test(μ₀, x̄, s, n, direction) / TInterval(x̄, s, n, C)",
    conclude:"Since p = ___ is [< / ≥] α, we [reject / FTR] H₀. [Convincing / not convincing] evidence that [Hₐ context].",
    mistake:"Using z* instead of t*, or forgetting to check for outliers when n is small." },
  { id:"2t", name:"2-Sample t", nodeId:"inf-means", color:"#dc2626",
    when:"Comparing two independent population means μ₁ − μ₂.",
    h0:"μ₁ − μ₂ = 0", ha:"μ₁ − μ₂ ≠ / < / > 0",
    conditions:["Random: both samples random/randomly assigned","Normal: both n ≥ 30, or both populations ~normal","Independent: samples independent of each other, both n < 10% of N"],
    testStat:"t = (x̄₁ − x̄₂) / √(s₁²/n₁ + s₂²/n₂)     df via Welch's or min(n₁−1, n₂−1)",
    ci:"(x̄₁ − x̄₂) ± t* · √(s₁²/n₁ + s₂²/n₂)",
    calc:"2-SampTTest / 2-SampTInt",
    conclude:"Since p = ___ is [< / ≥] α, we [reject / FTR] H₀. [Convincing / not convincing] evidence of a difference in [context].",
    mistake:"Pooling variances (not on AP exam) or using a paired test when groups are independent." },
  { id:"pt", name:"Paired t", nodeId:"inf-means", color:"#dc2626",
    when:"Comparing means from matched pairs (before/after, twins, same subject two conditions).",
    h0:"μd = 0 (mean difference = 0)", ha:"μd ≠ / < / > 0",
    conditions:["Random: pairs randomly selected or randomly assigned","Normal: n ≥ 30 differences, or differences ~normal","Independent: pairs independent, n < 10% of all possible pairs"],
    testStat:"t = (d̄ − 0) / (sd / √n)     df = n − 1  (n = number of PAIRS)",
    ci:"d̄ ± t* · (sd / √n)",
    calc:"T-Test on the list of differences",
    conclude:"Since p = ___ is [< / ≥] α, we [reject / FTR] H₀. [Convincing / not convincing] evidence that [mean difference context].",
    mistake:"Running a 2-sample t-test instead of pairing — loses the power of the paired design." },
  { id:"gof", name:"Chi-Square GOF", nodeId:"inf-chi", color:"#dc2626",
    when:"Testing if a single categorical variable follows a claimed distribution.",
    h0:"Distribution matches claimed proportions", ha:"Distribution does NOT match",
    conditions:["Random sample","Large Counts: all expected counts ≥ 5","Independent: n < 10% of population"],
    testStat:"χ² = Σ (O − E)² / E     df = categories − 1     Expected = n · p_claimed",
    ci:"(no CI for Chi-Square)",
    calc:"χ²GOF-Test",
    conclude:"Since p = ___ is [< / ≥] α, we [reject / FTR] H₀. [Convincing / not convincing] evidence that the distribution differs from [claimed].",
    mistake:"Checking observed counts ≥ 5 instead of expected counts ≥ 5." },
  { id:"chi2", name:"Chi-Square Ind/Hom", nodeId:"inf-chi", color:"#dc2626",
    when:"Testing association (independence) or same distribution across groups (homogeneity).",
    h0:"Variables are independent / distributions are the same", ha:"Variables are NOT independent / distributions differ",
    conditions:["Random sample(s)","Large Counts: all expected counts ≥ 5","Independent: n < 10% of population(s)"],
    testStat:"χ² = Σ (O − E)² / E     df = (r−1)(c−1)     Expected = (row total × col total) / grand total",
    ci:"(no CI for Chi-Square)",
    calc:"χ²-Test using matrix",
    conclude:"Since p = ___ is [< / ≥] α, we [reject / FTR] H₀. [Convincing / not convincing] evidence of [association / different distributions].",
    mistake:"Writing the wrong H₀ — independence vs. homogeneity have different wording even though math is identical." },
  { id:"lrt", name:"LinReg t", nodeId:"inf-regr", color:"#dc2626",
    when:"Testing if there is a linear relationship between x and y (slope ≠ 0).",
    h0:"β₁ = 0 (no linear relationship)", ha:"β₁ ≠ / < / > 0",
    conditions:["Linear: residual plot shows no pattern","Independent: observations independent","Normal: residuals ~normally distributed","Equal variance: residual spread constant (no fan)"],
    testStat:"t = b₁ / SE_b₁     df = n − 2     (values from computer output)",
    ci:"b₁ ± t* · SE_b₁     df = n − 2",
    calc:"LinRegTTest / LinRegTInt",
    conclude:"Since p = ___ is [< / ≥] α, we [reject / FTR] H₀. [Convincing / not convincing] evidence of a linear relationship between [x] and [y].",
    mistake:"Using n−1 instead of n−2 for df, or checking LINE from the scatterplot instead of the residual plot." },
];
